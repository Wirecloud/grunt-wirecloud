'use strict';

var chai = require('chai');
var expect = chai.expect;
var chaiAsPromised = require('chai-as-promised');
var WirecloudTask = require('../tasks/lib/utils.js');
var sinon = require('sinon');

chai.use(chaiAsPromised);

describe('Utils module', function () {

    beforeEach(function () {

    });

    it('should authenticate in the given IdM service', function () {

    });

    it('should upload the given MAC', function () {

    });

    it('should delete the given MAC', function () {

    });

    it('should check if a MAC already exists', function () {

    });

});
